# experiment
Open `cmd` in `./experiment` folder.  
Run commands below.  

```
python -m venv venv
```

```
activate-venv.bat
(venv) pip install mlflow
```

```
(venv) experiment.bat
```

```
(venv)mlflow ui
```